server:
  port: 8081
  servlet:
    context-path: /

camunda:
  client:
    mode: self-managed
    security:
      plaintext: true
    zeebe:
      enabled: true
      grpc-address: http://localhost:26500
      rest-address: http://localhost:8080

